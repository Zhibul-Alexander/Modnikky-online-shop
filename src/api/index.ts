import * as catalog from './catalog/catalog';
import * as bag from './bag/bag';
import * as subscribe from './subscribe/subscribe';

const API = {
  catalog,
  bag,
  subscribe,
};

export default API;